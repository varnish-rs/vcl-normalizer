[package]
name = "vcl-normalizer"
version = "0.1.0"
edition = "2021"
description = "VCL 4.1 functional-equivalence comparator"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
similar = "3"
anyhow = "1"

[dev-dependencies]
pretty_assertions = "1"

[features]
testutil = []
